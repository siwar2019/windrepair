import AddSettings from '../../components/settings/add-settings'

const addSettings = () => {
    return <AddSettings open={true} onClose={function (): void {
       
    } } />
}
export default addSettings
