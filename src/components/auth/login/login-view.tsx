import LoginForm from './Login-form'
import CustomBackground from '../view/custom-background'

const LoginView = () => {
    return <CustomBackground element={<LoginForm />} />
}
export default LoginView
